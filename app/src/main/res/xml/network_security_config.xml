<?xml version="1.0" encoding="utf-8"?>
<!--
  Restrict cleartext HTTP to local network only.
  All external traffic (Firebase, HuggingFace, GitHub CDN) uses HTTPS by default.
-->
<network-security-config>
    <!-- Localhost: allow cleartext for local MJPEG/HTTP API -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>

    <!-- LAN ranges: allow cleartext for camera phone WebRTC/MJPEG streaming -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">192.168.1.0</domain>
        <domain includeSubdomains="true">10.0.0.0</domain>
        <domain includeSubdomains="true">172.16.0.0</domain>
    </domain-config>

    <!-- All other traffic: HTTPS only -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>